{
  type: 'carousel',              // Type of carousel (e.g., 'carousel', 'slider')
  startAt: 0,                    // Index of the slide to start at
  autoplay: 3000,                // Time in milliseconds between automatic slide transitions
  gap: 0,                        // Gap between slides in pixels
  hoverpause: true,              // Pause autoplay on hover
  perView: 1,                    // Number of slides to show at once
  animationDuration: 800,        // Duration of the transition animation in milliseconds
  animationTimingFunc: 'linear'  // Timing function for the animation
}
